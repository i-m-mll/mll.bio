{"author":{"id":"2de4467f26c42fb223f4d3ddd5611e7c0513d3daae4c1dc315de039036bc997d"},"ops":[{"type":1,"timestamp":1778952283,"nonce":"9UXpXIlT1KKWOhyVoNqLR9Wc2lk=","title":"Restore linting and keep dependencies audit-clean","message":"## Problem\nThe current quality gate is not actually running lint checks, and dependency audit reports known vulnerabilities.\n\nCurrent evidence:\n- `npm run lint` fails before linting because `next lint` is deprecated/removed path and the project has Next 15 with `eslint-config-next@14`.\n- `next.config.mjs` sets `eslint.ignoreDuringBuilds = true`, so production builds skip linting.\n- `npm audit --omit=dev` reports vulnerabilities affecting `next`, `diff`, `postcss`, `js-yaml`, and `mdast-util-to-hast`.\n- `npm outdated --long` shows several core packages behind current compatible releases.\n\n## Context\nThis is a static-export site, so some Next server advisories may not be directly exploitable in production. Still, the repo should have a working lint command and a documented upgrade/audit baseline so future code review has signal.\n\n## Potential Solutions\n- Migrate from `next lint` to ESLint CLI and align `eslint-config-next` with the installed Next version.\n- Re-enable lint as a build/CI gate once it runs.\n- Run a targeted dependency update and document any remaining accepted audit findings.\n- Remove unused dependencies during the same pass where safe.\n\n---\nCo-Authored-By: Codex (GPT-5) \u003ccodex@openai.com\u003e","files":null}]}